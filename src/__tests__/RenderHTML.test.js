// import React from 'react';
// import { render } from '@testing-library/react';
// import RenderHTML from './RenderHTML';

// I wanted to try these, but I couldn't install jest for some reason. I got some
// critical error..

// test('renders paragraph for text type', () => {
//   const item = { type: 'text', html: 'This is a paragraph.' };
//   const { getByText } = render(<RenderHTML html={item.html} />);
//   const paragraphElement = getByText('This is a paragraph.');
//   expect(paragraphElement.tagName).toBe('P');
// });

// test('renders heading for subhead type', () => {
//   const item = { type: 'subhead', html: 'This is a subheading.' };
//   const { getByText } = render(<RenderHTML html={item.html} />);
//   const headingElement = getByText('This is a subheading.');
//   expect(headingElement.tagName).toBe('H2');
// });

// // Add more test cases for other item types as needed

